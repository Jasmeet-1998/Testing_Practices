const expect = require('chai').expect;

//==== Test Suite ====
describe('Mocha',()=>{
    // Test spec (unit test)
    it('should run all tests using npm',()=>{
        // expectations
        expect(true).to.be.ok; // ok is used to check some value is true or not
    });
});